import { IconType } from "react-icons";

export interface Iservices {
    title: string,
    about: string,
    Icon: IconType,
}

// for resume
export interface ISkill {
    name: string,
    level: string,
    Icon: IconType,
}